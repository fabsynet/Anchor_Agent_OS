---
phase: 09-founder-super-admin-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/api/src/admin/admin.module.ts
  - apps/api/src/admin/admin.controller.ts
  - apps/api/src/admin/admin.service.ts
  - apps/api/src/admin/audit/audit.service.ts
  - apps/api/src/admin/audit/audit.controller.ts
  - apps/api/src/auth/guards/super-admin.guard.ts
  - apps/api/src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "SuperAdminGuard validates token against super_admins table"
    - "GET /admin/metrics returns platform-wide counts"
    - "GET /admin/metrics/growth returns time series data"
    - "GET /admin/health returns health alerts"
    - "Every admin action is logged via AuditService"
    - "GET /admin/audit-logs returns paginated audit entries"
  artifacts:
    - path: "apps/api/src/auth/guards/super-admin.guard.ts"
      provides: "SuperAdmin authentication guard"
      min_lines: 30
    - path: "apps/api/src/admin/admin.service.ts"
      provides: "Platform metrics and health"
      exports: ["AdminService"]
    - path: "apps/api/src/admin/audit/audit.service.ts"
      provides: "Audit logging service"
      exports: ["AuditService"]
    - path: "apps/api/src/admin/admin.module.ts"
      provides: "Admin NestJS module"
      exports: ["AdminModule"]
  key_links:
    - from: "apps/api/src/auth/guards/super-admin.guard.ts"
      to: "packages/database (prisma.superAdmin)"
      via: "findUnique query"
      pattern: "prisma.superAdmin.findUnique"
    - from: "apps/api/src/admin/admin.service.ts"
      to: "packages/database (raw prisma)"
      via: "cross-tenant count queries"
      pattern: "this.prisma.(tenant|user|policy|client).count"
---

<objective>
Create the NestJS admin module with SuperAdminGuard, AuditService for action logging, and platform metrics/health endpoints.

Purpose: Backend foundation for all admin API endpoints — authentication, audit trail, and dashboard metrics.
Output: Working /admin/metrics, /admin/metrics/growth, /admin/health, /admin/audit-logs endpoints.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-founder-super-admin-dashboard/09-CONTEXT.md
@.planning/phases/09-founder-super-admin-dashboard/09-RESEARCH.md
@.planning/phases/09-founder-super-admin-dashboard/09-01-SUMMARY.md
@apps/api/src/auth/guards/jwt-auth.guard.ts
@apps/api/src/app.module.ts
@apps/api/src/common/prisma/prisma.service.ts
@apps/api/src/common/config/supabase.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SuperAdminGuard and AuditService</name>
  <files>
    apps/api/src/auth/guards/super-admin.guard.ts
    apps/api/src/admin/audit/audit.service.ts
    apps/api/src/admin/audit/audit.controller.ts
  </files>
  <action>
1. **SuperAdminGuard** (super-admin.guard.ts):
   - Implements CanActivate
   - Injects PrismaService and ConfigService
   - Extracts Bearer token from Authorization header
   - Validates token via Supabase auth.getUser() (same pattern as JwtAuthGuard)
   - Looks up user.id in prisma.superAdmin where isActive = true
   - If not found, throw ForbiddenException('Not a super-admin')
   - Sets request.user = { id, email, firstName, lastName, isSuperAdmin: true }
   - CRITICAL: Does NOT set CLS tenant context (admin queries are cross-tenant)
   - Uses createClient from @supabase/supabase-js with anon key (same as JwtAuthGuard pattern)

2. **AuditService** (audit.service.ts):
   - Injectable service
   - `log(params: { superAdminId, action, targetType, targetId, metadata?, ipAddress? })` — creates AdminAuditLog entry via this.prisma.adminAuditLog.create()
   - `getAuditLogs(filters: AuditLogFilters)` — paginated query with optional filters on action, targetType, superAdminId, date range. Include superAdmin relation (firstName, lastName, email). Order by createdAt DESC.
   - `getAuditLogStats()` — returns count by action type for the last 30 days

3. **AuditController** (audit.controller.ts):
   - @Controller('admin/audit-logs')
   - @UseGuards(SuperAdminGuard) on the controller
   - GET / — list audit logs with query params (action, targetType, superAdminId, startDate, endDate, page, limit)
   - GET /stats — audit log statistics
  </action>
  <verify>
TypeScript compiles: `cd apps/api && pnpm exec tsc --noEmit` passes.
  </verify>
  <done>SuperAdminGuard authenticates super-admins. AuditService logs and retrieves admin actions. Audit log endpoints available at /admin/audit-logs.</done>
</task>

<task type="auto">
  <name>Task 2: AdminModule with Platform Metrics and Health Endpoints</name>
  <files>
    apps/api/src/admin/admin.module.ts
    apps/api/src/admin/admin.controller.ts
    apps/api/src/admin/admin.service.ts
    apps/api/src/app.module.ts
  </files>
  <action>
1. **AdminService** (admin.service.ts):
   - Injects PrismaService (use raw this.prisma, NOT tenantClient)
   - `getPlatformMetrics()`: Cross-tenant counts using Promise.all:
     - totalAgencies: prisma.tenant.count()
     - totalUsers: prisma.user.count()
     - totalPolicies: prisma.policy.count()
     - totalClients: prisma.client.count()
     - totalPremiumValue: prisma.policy.aggregate({ _sum: { premium: true } }) on active/pending_renewal policies
   - `getPlatformMetricsForRange(start: Date, end: Date)`: Same counts but filtered by createdAt within range
   - `getGrowthTimeSeries(months: number)`: Loop through last N months, count new agencies/users/clients per month. Return array of { month: 'MMM yyyy', agencies, users, clients }. Use date-fns startOfMonth/endOfMonth/subMonths/format.
   - `getEngagementMetrics()`:
     - activeAgencies (agencies with users who have updatedAt in last 30 days)
     - inactiveAgencies (agencies with no user activity in 30+ days)
   - `getHealthAlerts()`: Array of HealthAlert objects:
     - Email failure rate: count EmailLog where status contains 'fail' / total EmailLog in last 24h. Alert if > 10%
     - Inactive agencies: count tenants where all users have updatedAt > 30 days ago. Alert with count
     - Storage warnings: (placeholder — return empty for now since storage tracking isn't implemented per-tenant)

2. **AdminController** (admin.controller.ts):
   - @Controller('admin')
   - @UseGuards(SuperAdminGuard) on the controller
   - GET /admin/metrics — returns PlatformMetrics (optional query params: startDate, endDate for range filtering)
   - GET /admin/metrics/growth?months=12 — returns growth time series
   - GET /admin/metrics/engagement — returns engagement metrics
   - GET /admin/health — returns health alerts array

3. **AdminModule** (admin.module.ts):
   - Imports: ConfigModule
   - Providers: AdminService, AuditService, SuperAdminGuard
   - Controllers: AdminController, AuditController
   - Export AuditService (needed by other admin sub-modules)

4. **app.module.ts**: Add AdminModule to imports array.

IMPORTANT: All queries use raw this.prisma (NOT tenantClient). Admin services have no CLS tenant context.
  </action>
  <verify>
`cd apps/api && pnpm exec tsc --noEmit` passes. `pnpm --filter api build` succeeds.
Start the API server and test: `curl http://localhost:3001/admin/metrics` returns 401 (no token) — confirming guard works.
  </verify>
  <done>Admin module registered in NestJS. Platform metrics, growth, engagement, and health endpoints functional behind SuperAdminGuard. AuditService available for all admin sub-modules.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` succeeds
- SuperAdminGuard rejects requests without valid super-admin token
- GET /admin/metrics returns platform counts
- GET /admin/metrics/growth returns time series
- GET /admin/health returns health alerts
- GET /admin/audit-logs returns paginated log entries
- AuditService.log() creates AdminAuditLog records
</verification>

<success_criteria>
- SuperAdminGuard validates Supabase token + super_admins table lookup
- Platform metrics endpoint returns cross-tenant aggregate counts
- Growth time series returns monthly data points
- Health alerts detect email failure rates and inactive agencies
- Audit log CRUD endpoints functional
- Admin module properly registered in app.module.ts
</success_criteria>

<output>
After completion, create `.planning/phases/09-founder-super-admin-dashboard/09-03-SUMMARY.md`
</output>
